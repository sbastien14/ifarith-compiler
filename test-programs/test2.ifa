;; Program 3: Factorial Calculation
(define (factorial n)
  (if (<= n 1)
      1
      (* n (factorial (- n 1)))))

(define (main)
  (define num 5)
  (define result (factorial num))
  (print "The factorial of" num "is" result))