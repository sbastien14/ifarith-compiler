;; Program 4: Fibonacci Sequence
(define (fibonacci n)
  (if (or (= n 0) (= n 1))
      n
      (+ (fibonacci (- n 1)) (fibonacci (- n 2)))))

(define (main)
  (define num 8)
  (define result (fibonacci num))
  (print "The Fibonacci sequence value at position" num "is" result))
